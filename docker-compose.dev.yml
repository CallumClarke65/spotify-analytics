version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: spotify-analytics-dev
    working_dir: /app
    volumes:
      - .:/app
      - ./docs:/app/docs
    ports:
      - "8080:8080"
